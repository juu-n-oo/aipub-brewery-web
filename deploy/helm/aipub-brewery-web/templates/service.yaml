apiVersion: v1
kind: Service
metadata:
  name: {{ include "aipub-brewery-web.fullname" . }}
  labels:
    {{- include "aipub-brewery-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aipub-brewery-web.selectorLabels" . | nindent 4 }}
