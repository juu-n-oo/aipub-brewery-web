apiVersion: v1
kind: Service
metadata:
  name: {{ include "dockerizer-web.fullname" . }}
  labels:
    {{- include "dockerizer-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dockerizer-web.selectorLabels" . | nindent 4 }}
