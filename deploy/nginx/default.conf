server {
    listen       80;
    server_name  _;

    # Vite hashed assets — long-term cache
    location /dockerizer/assets/ {
        alias /usr/share/nginx/html/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # SPA fallback — sub path
    location /dockerizer/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /dockerizer/index.html;
    }

    # /dockerizer (trailing slash 없이 접근 시)
    location = /dockerizer {
        return 301 /dockerizer/;
    }

    # Health check
    location /healthz {
        access_log off;
        return 200 "ok";
    }
}
