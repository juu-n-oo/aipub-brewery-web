apiVersion: v1
kind: Service
metadata:
  name: aipub-brewery-web
  namespace: aipub
  labels:
    app.kubernetes.io/name: aipub-brewery-web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: aipub-brewery-web
